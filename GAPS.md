# GAPS — mcp-shim

## Known Gaps

No additional functional gaps are currently tracked here. The CLI path, relative import
resolution, default-export fallback, and missing-plugin error path are now covered by the
automated test suite.
